import Link from "next/link";
import { FileText, FolderOpen, ReceiptText } from "lucide-react";
export function Dashboard({project,invoice,documentCount}:{project?:{name:string;status:string};invoice?:{id:string;publicId:string;amount:string;currency:string};documentCount:number}){if(!project)return <div className="content"><section className="page-heading"><div><p className="eyebrow">REZO PORTAL</p><h1>Your workspace is not assigned yet.</h1><p className="lead">Your administrator will add you to a project when it is ready.</p></div></section><section className="empty-state"><span className="document-icon"><FolderOpen size={22}/></span><h2>No workspace available</h2><p>There is nothing you need to do here yet. Project records and documents will appear automatically after assignment.</p></section></div>;return <div className="content"><section className="page-heading"><div><p className="eyebrow">REZO PORTAL</p><h1>{project.name}</h1><p className="lead">Your assigned project workspace.</p></div></section><section className="project-banner"><div className="project-index">01</div><div className="project-copy"><p className="eyebrow">PROJECT STATUS</p><h2>{project.status.replaceAll("_"," ")}</h2><p>Documents, invoices, and files are available from the workspace navigation.</p></div></section><section className="dashboard-grid"><div className="main-column"><div className="section-title"><div><p className="eyebrow">WORKSPACE</p><h2>Available records</h2></div></div><div className="document-list"><Link className="document-row" href="/project"><span className="document-icon"><FolderOpen size={18}/></span><span className="document-name"><b>Project details</b><small>Scope and delivery status</small></span></Link><Link className="document-row" href="/documents"><span className="document-icon"><FileText size={18}/></span><span className="document-name"><b>Documents</b><small>{documentCount} available</small></span></Link><Link className="document-row" href="/invoices"><span className="document-icon"><ReceiptText size={18}/></span><span className="document-name"><b>Invoices</b><small>{invoice?`${invoice.publicId} · ${invoice.currency} ${invoice.amount}`:"No payment due"}</small></span></Link></div></div></section></div>}
