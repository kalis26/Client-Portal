"use client";
import { FormEvent, useState } from "react";
export function AccountSettings({ name, locale }: { name: string; locale: string }) { const [note,setNote]=useState(""); async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);const r=await fetch("/api/account",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:form.get("name"),locale:form.get("locale")})});setNote(r.ok?"Profile saved.":"Could not save profile.")} async function logout(){await fetch("/api/account",{method:"DELETE"});location.assign("/sign-in")} return <section className="setup-card"><form className="setup-form" onSubmit={save}><div className="form-grid"><label>Name<input name="name" defaultValue={name} required/></label><label>Language<select name="locale" defaultValue={locale}><option value="fr">Français</option><option value="en">English</option></select></label></div><button className="button">Save profile</button>{note&&<p className="form-success">{note}</p>}</form><hr/><button className="button button-quiet" onClick={logout}>Sign out of this session</button></section> }
