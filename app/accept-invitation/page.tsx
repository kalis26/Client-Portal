"use client";import { FormEvent,Suspense,useState } from "react";import Link from "next/link";import { useSearchParams } from "next/navigation";function InvitationForm(){const q=useSearchParams(),[error,setError]=useState(""),[done,setDone]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);if(f.get("password")!==f.get("confirm"))return setError("Passwords do not match.");const r=await fetch("/api/auth/accept-invitation",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token:q.get("token"),password:f.get("password")})});if(r.ok)setDone(true);else setError((await r.json()).error??"Could not accept invitation.")}return <main className="sign-in"><section><p className="eyebrow">REZO</p><h1>Set up your workspace.</h1>{done?<p>Account ready. <Link href="/sign-in">Sign in</Link></p>:<form onSubmit={submit}><label>New password<input name="password" type="password" minLength={12} required/></label><label>Confirm password<input name="confirm" type="password" minLength={12} required/></label>{error&&<p className="form-error">{error}</p>}<button className="button">Activate account</button></form>}</section></main>}export default function AcceptInvitation(){return <Suspense><InvitationForm/></Suspense>}
