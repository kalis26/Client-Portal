import Link from "next/link";import { and,desc,eq } from "drizzle-orm";import { requireAdmin } from "@/lib/authz";import { db } from "@/lib/db";import { auditEvents,clientContacts,clients,generatedDocuments,invoices,payments,projects } from "@/lib/db/schema";import { PortalShell } from "@/components/portal-shell";
export default async function ProjectDetail({params}:{params:Promise<{projectId:string}>}){await requireAdmin();const id=(await params).projectId;const row=(await db.select({project:projects,client:clients.legalName}).from(projects).innerJoin(clients,eq(projects.clientId,clients.id)).where(eq(projects.id,id)).limit(1))[0];if(!row)return <PortalShell role="admin"><div className="content"><h1>Project not found</h1></div></PortalShell>;const [contacts,bills,docs,events]=await Promise.all([db.select().from(clientContacts).where(eq(clientContacts.clientId,row.project.clientId)),db.select().from(invoices).where(eq(invoices.projectId,id)),db.select().from(generatedDocuments).where(eq(generatedDocuments.projectId,id)),db.select().from(auditEvents).where(eq(auditEvents.entityId,id)).orderBy(desc(auditEvents.createdAt))]);return <PortalShell role="admin"><div className="content"><section className="page-heading"><div><p className="eyebrow">{row.project.publicId} · {row.project.status}</p><h1>{row.project.name}</h1><p className="lead">{row.client}</p></div><Link className="button button-quiet" href="/admin/projects">All projects</Link></section><section className="record-list"><div className="record-row"><span><b>Scope</b><small>{row.project.scope}</small></span></div>{contacts.map(c=><div className="record-row" key={c.id}><span><b>{c.name}</b><small>{c.email} · {c.userId?"portal account linked":"invitation pending"}</small></span></div>)}{bills.map(b=><div className="record-row" key={b.id}><span><b>{b.publicId}</b><small>{b.isDeposit?"Deposit":"Final"} invoice</small></span><strong>{b.currency} {b.amount}</strong><em className="status status-warning">{b.status}</em></div>)}{docs.map(d=><div className="record-row" key={d.id}><span><b>{d.kind}</b><small>{d.templateVersion}</small></span><a className="button button-quiet" href={`/api/documents/${d.id}/download`}>Download</a></div>)}{events.map(e=><div className="record-row" key={e.id}><span><b>{e.action}</b><small>{e.createdAt.toLocaleString()}</small></span></div>)}</section></div></PortalShell>}
